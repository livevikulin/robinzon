//- ====================
//- RBZ: button block link
//- 16.01.2018: Amedomary
//- --------------------
//- Блочная ссылка
//- ====================

//- if <a>        link = true
//- if <button>   link = false
//- add_text - если есть доп текст, для мобилки например

//- Вызов
//- +button('text', 'add-text', false).js-class.-active(data-popup="open-test")

mixin button(content, add_text, link)
    if link == true
        +b('a').button( href="javascript:void(0);" title=`${content}` )&attributes( attributes ) #{content}
            if add_text
                +e.add-text(class!=attributes.class) #{add_text}
            block

    else
        +b('button').button&attributes( attributes ) #{content}
            if add_text
                +e.add-text(class!=attributes.class) #{add_text}
            block
